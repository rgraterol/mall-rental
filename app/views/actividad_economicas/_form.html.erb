<%= form_for(@actividad_economica) do |f| %>
  <% if @actividad_economica.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@actividad_economica.errors.count, "error") %> prohibited this actividad_economica from being saved:</h2>

      <ul>
      <% @actividad_economica.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :nombre %><br>
    <%= f.text_field :nombre %>
  </div>

  <div class="actions">
    <%= f.submit 'Guardar', :class => 'btn btn-primary' %>
  </div>
<% end %>
